name: cubic_flutter_assessment
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.10.0

dependencies:
  flutter:
    sdk: flutter
  firebase_core:
  firebase_auth:
  flutter_bloc:
  equatable:
  dio:
  flutter_secure_storage:
  skeletonizer:
  injectable:
  get_it:

  url_launcher:
  firebase_crashlytics:
  flutter_screenutil:
  location:
  google_maps_flutter:
  permission_handler:
  flutter_native_splash:
  shared_preferences:
  hive:
  hive_flutter:
  easy_localization:
  flutter_svg:
  image_picker:
  cherry_toast:
  fluttertoast:
  cloud_firestore:
  screen_protector:
  local_auth: ^3.0.0
  connectivity_plus:
  logger:


  intl_phone_field:

  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner:
  injectable_generator:
  flutter_lints: ^6.0.0
  hive_generator: ^2.0.1

flutter:

  uses-material-design: true

  assets:
    - assets/images/
    - assets/svgs/
    - assets/translations/
